<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.spiritsandwineapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SpiritsAndWineApp">
        <activity android:name=".ui.SpiritAndWinesDetail"></activity>
        <activity android:name=".ui.SpiritAndWinesList" />
        <activity android:name=".SpiritandWineDetailActivity" />
        <activity android:name=".ui.SpiritAndWineListActivity" />
        <activity android:name=".ui.MainActivity" />
    </application>

</manifest>